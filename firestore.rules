rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    function isSignedIn() {
      return request.auth != null;
    }

    // Replace this with your real admin email before publishing rules.
    function isAdmin() {
      return isSignedIn() && request.auth.token.email == "CHANGE_THIS_TO_YOUR_EMAIL@example.com";
    }

    function isOwner() {
      return isSignedIn() && request.auth.uid == resource.data.userId;
    }

    match /resumes/{resumeId} {
      // Any logged-in user can create a resume for themselves
      allow create: if isSignedIn() && request.resource.data.userId == request.auth.uid;

      // Owner can read, update, delete their own resume
      allow read, update, delete: if isOwner();

      // Admin can read and delete ANY resume (for admin.html)
      allow read, delete: if isAdmin();
    }

  }
}
